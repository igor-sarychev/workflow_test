name: "App_a and common"

"on":
  push:
    branches:
      - main
    paths:
      - "app_a/**"
      - "common/**"

jobs:
  main:
    runs-on: "self-hosted"
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v3
